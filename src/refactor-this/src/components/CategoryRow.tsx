import React from "react";

interface CategoryRowProps {
  row: number;
  category: string;
}

const CategoryRow = ({ row, category }: CategoryRowProps) => {
  return <div></div>;
};

export default CategoryRow;
